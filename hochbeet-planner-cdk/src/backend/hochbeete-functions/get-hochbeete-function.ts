// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for GetHochbeeteFunction
 */
export interface GetHochbeeteFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/backend/hochbeete-functions/get-hochbeete.
 */
export class GetHochbeeteFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: GetHochbeeteFunctionProps) {
    super(scope, id, {
      description: 'src/backend/hochbeete-functions/get-hochbeete.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/backend/hochbeete-functions/get-hochbeete.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}